/**
 * This package contains the classes responsible for merging intermediate representations (IR) with delta changes.
 *
 * <p>The primary class in this package is {@link edu.university.ecs.lab.intermediate.merge.IRMergeRunner}, which serves
 * as the main entry point for initiating the IR merge process. This process involves using the {@link edu.university.ecs.lab.intermediate.merge.services.MergeService}
 * to merge an existing intermediate representation with delta changes to produce an updated intermediate representation.</p>
 */
package edu.university.ecs.lab.intermediate.merge;
